using UnityEngine;
using UnityEngineEx;

public partial class js /*.hero*/
{
    /*
    [HideInInspector] public hero_ hero = new hero_();

    public void SetHero(byte ally, Vector3 pos, Vector3 dir, int zoneIdx,
        int numAmmo1, int numAmmo2, int numBomb)
    {
        hero.ally = ally;
        hero.pos = pos;
        hero.dir = dir;
        hero.zoneIdx = zoneIdx;
        hero.numAmmoPistol = numAmmo1;
        hero.numAmmoRifle = numAmmo2;
        hero.numBomb = numBomb;

        if (null == js.Inst.hero.hud)
            hero.hud = core.huds.editmap.CreateCel1l(js.Inst.hero.pos, Color.white, 0.1f, "hero(Clone)");
        else
            hero.hud.ActiveRectOnXZ(pos.WithGapY(0.01f));
    }

    [System.Serializable]
    public class hero_
    {
        [SerializeField] public byte ally = 0;
        [HideInInspector] public Vector3 pos;
        [SerializeField] public Vector3 dir = new Vector3(0, 0, 1);
        [SerializeField] public int zoneIdx = 0;

        [SerializeField] public int numAmmoPistol = 10;
        [SerializeField] public int numAmmoRifle = 30;
        [SerializeField] public int numBomb = 1;
        
        [HideInInspector] public hudLine hud;
        
    }*/
}
